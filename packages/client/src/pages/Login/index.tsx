import { Page } from '@Types';

const LoginPage: React.FC = () => {
  const a = '';
  return <div>LoginPage</div>;
};

export default {
  title: 'Login',
  path: '/login',
  element: LoginPage,
  protected: false,
} as Page;
