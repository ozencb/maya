import { Page } from '@Types';

const RegisterPage: React.FC = () => {
  return <div>RegisterPage</div>;
};

export default {
  title: 'Register',
  path: '/register',
  element: RegisterPage,
  protected: false,
} as Page;
