export const success = { success: true };
export const error = { success: false };

export enum Status {
  SUCCESS = 200,
  ERROR = 500,
  NOT_FOUND = 404,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  BAD = 400,
}
